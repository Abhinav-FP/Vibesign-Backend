export enum UserRole {
    Customer = "customer",
    Partner = "partner",
    Admin = "admin",
    SuperAdmin = "superAdmin",
}

export enum MediaType {
    File = "file",
    Directory = "directory",
}
